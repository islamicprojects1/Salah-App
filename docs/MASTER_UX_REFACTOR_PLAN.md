# خطة شاملة لتطوير واجهة المستخدم وتصليح التطبيق

وثيقة مرجعية لتحديد الميزات التي تُعاد كتابتها من الصفر، وتصليح المشاكل، وتحسين تجربة المستخدم.

---

## 1. الصفحة الرئيسية (Dashboard) — إعادة هندسة

### 1.1 الهدف
- **صفحة بدون سكرول**: كل المحتوى يظهر دون تمرير
- **تحسين استغلال المساحة**: لا هدر للمساحات

### 1.2 الملخص اليومي (Daily Summary)
**الوضع الحالي:** بطاقة ثابتة ضمن التمرير  
**المطلوب:** صفحة منبثقة (Overlay/Bottom Sheet) شفافة من الحواف

- تطفو فوق الصفحة الرئيسية
- حواف شفافة تُظهر المحتوى تحتها
- إغلاق: سحب لأعلى أو للأسفل (مثل Page X)
- أو زر إغلاق واضح
- يمكن فتحها من أيقونة/زر صغير في الهيدر أو بجانب الساعة

**ملف مقترح:** `lib/features/prayer/presentation/widgets/daily_summary_overlay.dart`

### 1.3 المحتوى النهائي للصفحة الرئيسية (بدون سكرول)

| العنصر | الحجم/الملاحظات |
|--------|------------------|
| AppBar | المدينة + أيقونة القبلة (مدمجة) |
| الساعة الكبيرة | سطر واحد |
| الستريك | **نفس سطر الساعة** (يمين الساعة أو تحتها مباشرة بسطر واحد) |
| دائرة الصلاة (SmartPrayerCircle) | عنصر رئيسي |
| الصلوات السريعة (أيقونات أفقية) | صف واحد مضغوط |
| مؤشر الاتصال | صغير جداً، ركن ثابت |
| زر/أيقونة فتح الملخص اليومي | صغير |

**ملف يُعاد كتابته:** `dashboard_screen.dart` — تحويل SingleChildScrollView إلى Layout ثابت (Column + Expanded أو CustomScrollView بدون scroll فعلَي).

---

## 2. صفحة العائلة — إصلاحات وتصغير

### 2.1 إلغاء الساعة الصغيرة
**المشكلة:** ساعة صغيرة (AM 7:28 في شكل بيضوي) مكررة — الساعة موجودة في أعلى الصفحة  
**الحل:** إزالة أي ودجت يعرض الساعة بشكل منفصل داخل صفحة العائلة.

**الملفات للمراجعة:** `family_dashboard_screen.dart`, `family_flame_widget.dart`, `family_vitality_orb.dart`

### 2.2 الستريك والساعة في سطر واحد
**المطلوب:** وضع الستريك (🔥 X يوم) في **نفس السطر** مع الساعة، لا كل واحد في سطر منفصل.

**مثال التخطيط:**
```
[7:29 ص]                    [🔥 0 يوم]
```

### 2.3 بطاقات الصلوات — تلوين حسب التوقيت
**الوضع الحالي:** موجودة في `PrayerTimingHelper`, `PrayerTimingQuality`  
**المطلوب:**

| الحالة | اللون |
|--------|-------|
| في الوقت | أخضر |
| متأخر | برتقالي/أصفر |
| فائتة | أحمر |
| مستقبلية | رمادي فاتح |
| جاري (نافذة الحالية) | لون مميز (ذهبي/أزرق) |

التطبيق على:
- أيقونات الصلوات في الـ Timeline
- بطاقات الصلوات في صفحة العائلة (قسم الأعضاء)
- الودجت (إن وُجد)

### 2.4 تصغير هيدر العائلة

**المشكلة:** كود الدعوة، الإشعارات، إضافة طفل — عناصر كبيرة تأخذ نصف الشاشة.

**الحلول:**

| العنصر | التعديل |
|--------|---------|
| كود الدعوة | نص أصغر، أيقونة مشاركة صغيرة، سطر واحد |
| زر الإشعارات | أيقونة في الهيدر فقط (موجود) |
| إضافة طفل | تغيير النص إلى: **"إضافة ابن لا يملك هاتف"** وتصغير الزر إلى حجم Chip أو زر نصي صغير |

**ترجمة جديدة:**
- `add_child_no_phone_btn` → "إضافة ابن لا يملك هاتف" (عربي)
- "Add child without phone" (إنجليزي)

**تصميم مقترح للهيدر:**
```
[اسم العائلة ▾]     [🔔]
[كود: 85GYG4 📤]  [إضافة ابن لا يملك هاتف]  ← كلها في سطرين أو أقل، compact
```

---

## 3. نبض العائلة والإشعارات — إعادة تصميم من الصفر

### 3.1 تقييم الوضع الحالي
- الشكل: "نبض العائلة" + عناصر متكررة (مثل: Mohammad Reziq صلى الفجر 11 س)
- المنطق والـ UI بحاجة لإعادة تصميم

### 3.2 خطة إعادة هندسة النبض

**المنطق (Logic):**
- تجميع الأحداث حسب النوع (صلاة، تشجيع، إكمال يوم، احتفال)
- تجميع أحياناً: "3 من العائلة صلوا العصر" بدل عرض 3 أسطر منفصلة
- تصفية حسب الزمن (مثلاً آخر 24 ساعة)
- دعم التحديث الفوري (Stream من Firestore)

**الواجهة (UI/UX):**
- عرض مختصر: "من صلى ماذا" في شكل Chips أو أسطر قصيرة
- ألوان مختلفة لكل نوع (صلاة = أخضر، تشجيع = أزرق، إكمال = ذهبي، احتفال = كهرماني)
- توسيع للتفاصيل: عند الضغط يفتح صفحة/قائمة بأحداث اليوم
- عرض الوقت النسبي (منذ 5 دقائق، منذ ساعة، إلخ)

**الملفات:**
- `family_pulse_section.dart` — إعادة كتابة
- `family_pulse_model.dart` — مراجعة
- `family_service.dart` (getPulseStream, addPulseEvent) — تحسين التجمع والتصفية

### 3.3 خطة الإشعارات
- شاشة الإشعارات: تبويبات (كل الإشعارات | عائلية فقط | تشجيع)
- إشعارات فورية: عند "من صلى" — احترام إعداد `family_notification`
- ربط مع FCM عند جاهزية الـ Backend

---

## 4. الثيم (Dark Mode) — تصليح شامل

### 4.1 المشكلة
عند التبديل للوضع الداكن، بعض العناصر تتغير وبعضها لا.

### 4.2 السبب الشائع
- استخدام ألوان ثابتة (`Colors.white`, `Color(0xFF...)`) بدل ألوان الثيم
- استخدام `AppColors.primary` بدون مراعاة الخلفية والنص
- بعض الودجتات لا تستخدم `Theme.of(context)` أو `colorScheme`

### 4.3 الحل
1. **مراجعة كل الملفات** التي تستخدم:
   - `Colors.white` / `Colors.black` → استبدال بـ `colorScheme.surface`, `colorScheme.onSurface`
   - ألوان ثابتة للخلفية/النص → استخدام `Theme.of(context).colorScheme`
2. **استخدام `AppColors` الديناميكية فقط:**
   - `AppColors.background`, `AppColors.surface`, `AppColors.textPrimary`, `AppColors.textSecondary`
   - التأكد أن هذه القيم تعتمد على `Get.isDarkMode` أو `Theme.of(context).brightness`
3. **ملفات للمراجعة:**
   - `app_colors.dart`
   - `app_theme.dart`
   - `CustomDrawer`
   - `DashboardAppBar`
   - `FamilyHeaderCard`
   - `SmartPrayerCircle`
   - `DailyReviewCard`
   - أي Card أو Container له لون خلفية ثابت

### 4.4 قائمة تدقيق الثيم

| العنصر | Light | Dark | الحالة |
|--------|-------|------|--------|
| الخلفية | #F5F5F5 | #121212 | مراجعة |
| البطاقات | #FFFFFF | #2C2C2C | مراجعة |
| النص الرئيسي | #212121 | #E0E0E0 | مراجعة |
| النص الثانوي | #757575 | #9E9E9E | مراجعة |
| الحدود والـ Dividers | #BDBDBD | #424242 | مراجعة |
| الدراور | | | مراجعة |
| هيدر العائلة (أخضر) | | | قد يبقى ثابت مع تعديل contrast |

---

## 5. ميزات يُقترح إعادة كتابتها من الصفر

| الميزة | السبب | الأولوية |
|--------|-------|----------|
| **صفحة العائلة (Family Dashboard)** | تكرار عناصر، هيدر كبير، نبض ضعيف، تخطيط غير واضح | عالية |
| **نبض العائلة (Family Pulse)** | منطق وواجهة تحتاج إعادة تصميم | عالية |
| **شاشة الإشعارات** | ربطها مع النبض وتحسين التبويبات والتجربة | متوسطة |
| **الصفحة الرئيسية (Dashboard Home)** | تحويل لصفحة بدون سكرول + ملخص منبثق | عالية |
| **نظام الثيم** | تصليح Dark Mode جذري | عالية |
| **الدراور (Drawer)** | مراجعة تبعاً للثيم والألوان | متوسطة |

---

## 6. ترتيب التنفيذ المقترح

### المرحلة 1 — تصليح الثيم
1. مراجعة `AppColors` و `AppTheme`
2. استبدال كل الألوان الثابتة بألوان ديناميكية
3. اختبار التبديل بين فاتح/داكن على كل الشاشات

### المرحلة 2 — الصفحة الرئيسية
1. إلغاء الـ Scroll وجعل المحتوى ثابتاً
2. وضع الساعة والستريك في سطر واحد
3. تحويل الملخص اليومي إلى Overlay/Bottom Sheet شفاف
4. تصغير العناصر لتجنب الازدحام

### المرحلة 3 — صفحة العائلة
1. إزالة الساعة الصغيرة
2. تصغير الهيدر (كود الدعوة، إضافة ابن لا يملك هاتف)
3. توحيد سطر الساعة والستريك
4. التأكد من تلوين بطاقات الصلوات حسب التوقيت

### المرحلة 4 — نبض العائلة والإشعارات
1. إعادة كتابة منطق النبض (تجميع، تصفية)
2. إعادة تصميم واجهة النبض
3. تحسين شاشة الإشعارات وربطها بالنبض

---

## 7. ملخص الملفات المتأثرة

```
lib/
├── core/
│   ├── theme/
│   │   ├── app_colors.dart      ← تصليح
│   │   └── app_theme.dart       ← مراجعة
│   └── constants/
│       └── app_dimensions.dart  ← مراجعة المسافات
├── features/
│   ├── prayer/
│   │   └── presentation/
│   │       ├── screens/
│   │       │   └── dashboard_screen.dart     ← إعادة هيكلة
│   │       └── widgets/
│   │           ├── daily_review_card.dart    ← تحويل لـ Overlay
│   │           └── smart_prayer_circle.dart  ← مراجعة ثيم
│   ├── family/
│   │   └── presentation/
│   │       ├── screens/
│   │       │   └── family_dashboard_screen.dart  ← إعادة كتابة
│   │       └── widgets/
│   │           ├── family_header_card.dart       ← تصغير
│   │           ├── family_pulse_section.dart     ← إعادة كتابة
│   │           ├── family_flame_widget.dart      ← مراجعة
│   │           └── family_vitality_orb.dart      ← مراجعة
│   ├── notifications/
│   │   └── presentation/...     ← تحسين
│   └── settings/
│       └── data/services/
│           └── theme_service.dart   ← مراجعة
└── core/localization/
    ├── ar_translations.dart     ← add_child_no_phone_btn
    └── en_translations.dart     ← add_child_no_phone_btn
```

---

**آخر تحديث:** 2026-02-16
