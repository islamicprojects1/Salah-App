# المواصفة الشاملة: الإشعارات، التواصل، والنجاح — قُرب

وثيقة نظرية وتصميمية كاملة لنظام الإشعارات، التفاعل بين المستخدمين، والتعامل مع كل الحالات لضمان ربط الناس بنجاح واستخدام التطبيق فعلياً.

---

## 1. رؤية التطبيق وكيف ينجح

### الهدف
**ربط الناس روحياً وجماعياً حول الصلاة** — ليس فقط تتبعاً فردياً، بل عائلة تذكر بعضها وتشجع بعضها وتشعر بأنها تصلي معاً.

### لماذا سيستخدم الناس التطبيق؟
| القيمة | الشرح |
|--------|--------|
| **تذكير لطيف** | أذان في وقته + تذكير واحد ذكي (بدون إزعاج متكرر). |
| **شعور جماعي** | معرفة أن "العائلة صلّت" أو "أحمد صلى العصر" يعطي دافعاً ومسؤولية جماعية. |
| **تشجيع بدون ضغط** | زر "شجّعه" أو "بارك الله فيك" يصل كإشعار أو يظهر في الـ Pulse — لا رسائل طويلة. |
| **تسجيل سريع** | من الإشعار "صليت" أو من الشاشة الرئيسية — أقل احتكاك ممكن. |
| **احترام الخصوصية** | إعدادات واضحة: من يرى ماذا (عائلة فقط، أو مجهول)، وإيقاف الإشعارات العائلية إن رغب. |

### مبادئ التصميم
1. **الإشعارات مساعدة لا إزعاج** — نرسل عند الحاجة فقط، ونلغي التذكير فور تسجيل الصلاة.
2. **الرسائل داخل التطبيق بسيطة** — لا دردشة مفتوحة؛ تفاعل عبر "تشجيع"، "نبض العائلة"، وإشعارات محدودة.
3. **الأب/الأم مركز العائلة** — يستطيع تسجيل الصلاة عن الطفل، رؤية ملخص اليوم، وإرسال تشجيع.
4. **كل حالة لها سلوك واضح** — لا موقع؟ نعرض رسالة واضحة ونقدّم خيار اختيار مدينة يدوياً. لا إنترنت؟ نعمل أوفلاين ونوضح "سيتم المزامنة لاحقاً".

---

## 2. نظام الإشعارات الكامل

### 2.1 أنواع الإشعارات ومتى تُرسل

| النوع | متى | النص (مثال عربي) | الإجراءات (أزرار) |
|-------|-----|-------------------|---------------------|
| **أذان** | عند دخول وقت الصلاة (حسب مواقيت الموقع أو المدينة المختارة) | "حان وقت العصر 🕌 — هيا لنصلي معاً" | ✅ صليت \| ⏰ لاحقاً (5/10/15 دقيقة) |
| **تذكير واحد** | بعد X دقيقة من الأذان (مثلاً 30) — **فقط إن لم يُسجّل المستخدم الصلاة بعد** | "تذكير: العصر — لم تُسجّل بعد" | ✅ صليت \| ⏰ لاحقاً |
| **تذكير لاحقاً** | عند ضغط "لاحقاً" يُجدول تذكير بعد 5/10/15 دقيقة حسب الاختيار | نفس الصلاة، نفس الأزرار | ✅ صليت \| ⏰ لاحقاً |
| **عائلي (اختياري)** | عند تفعيل "إشعارات العائلة": عندما يسجّل عضو من العائلة صلاة | "أحمد صلّى العصر" أو "3 من أسرتك صلّوا العصر" | فتح التطبيق → تبويب العائلة |
| **تشجيع** | عندما يضغط أحد (مثلاً الأب) "شجّعه" على عضو — يُرسل للمُشجَّع إشعار (إن كان مفعّلاً) | "والدك يرسل لك تشجيعًا 🌟 — بارك الله فيك" | فتح التطبيق |
| **قضاء (Qada)** | عند اكتشاف صلوات فائتة (مثلاً من QadaDetectionService) — مرة واحدة في اليوم أو عند فتح التطبيق | "لديك صلوات لم تُسجّل — هل تريد تعويضها؟" | فتح شاشة القضاء |

### 2.2 قواعد الجدولة والإلغاء

- **إلغاء تلقائي**: بمجرد تسجيل المستخدم للصلاة (من التطبيق أو من زر "صليت" في الإشعار)، يُلغى أي إشعار أذان أو تذكير لهذه الصلاة.
- **عدم التكرار**: لا نرسل أكثر من تذكير واحد "عام" لكل صلاة (مثلاً تذكير 30 دقيقة). زر "لاحقاً" يولد تذكيراً إضافياً لمرة واحدة (5/10/15).
- **صوت الإشعار**: حسب إعداد المستخدم: أذان كامل، اهتزاز فقط، أو صامت — ويُطبَّق على قنوات Android المناسبة.

### 2.3 إعدادات الإشعارات (مصدر واحد)

يجب أن تكون كل الخيارات مخزنة في Storage (مفاتيح في `StorageKeys`) ومربوطة بواجهة الإعدادات:

| الإعداد | المفتاح (مثال) | القيمة الافتراضية |
|---------|-----------------|---------------------|
| تفعيل الإشعارات كلياً | `notifications_enabled` | true |
| إشعارات الأذان | `adhan_notifications_enabled` | true |
| تذكير بعد X دقيقة | (ثابت أو إعداد) | 30 |
| إشعارات العائلة | `family_notification` | true (للأب)، false أو true حسب المستخدم |
| وضع الصوت | `notification_sound_mode` | adhan / vibrate / silent |
| تفعيل كل صلاة (فجر، ظهر، …) | `fajr_notification`, … | true لكل صلاة |

عند **أول تشغيل** أو عند **إيقاف الإشعارات** من النظام، نعرض رسالة واحدة واضحة: "لتفعيل تذكير الصلاة، اسمح بالإشعارات من إعدادات الجهاز".

---

## 3. التواصل والتفاعل بين المستخدمين

### 3.1 الأدوار

| الدور | الصلاحيات والتفاعل |
|-------|---------------------|
| **فرد (بدون عائلة)** | تسجيل صلواته فقط؛ إشعارات أذان وتذكير شخصية. لا رسائل بين مستخدمين. |
| **أب/أم (منشئ عائلة)** | إنشاء عائلة، دعوة أطفال، رؤية ملخص اليوم لكل عضو، تسجيل صلاة عن طفل، إرسال "تشجيع" لأي عضو. استلام إشعارات عائلية (من صلى). |
| **طفل/عضو في عائلة** | رؤية ملخص العائلة و"نبض العائلة"، استلام تشجيع (إشعار + ظهور في Pulse). لا يستطيع تسجيل صلاة عن غيره (إلا إن تم توسيع الصلاحيات لاحقاً). |

### 3.2 قنوات "التواصل" (بدون دردشة مفتوحة)

| القناة | الوصف | أين يظهر |
|--------|--------|----------|
| **نبض العائلة (Pulse)** | أحداث: "من صلى ماذا"، "من شجّع من"، "إكمال يومي". | صفحة العائلة — قسم "نبض العائلة" أو "آخر النشاط". |
| **تشجيع** | زر "شجّعه" أو "بارك الله فيك" من الأب أو من عضو. | إشعار للمُشجَّع (إن مُفعّل) + حدث في Pulse. |
| **إشعار عائلي** | "فلان صلّى X" أو "3 من أسرتك صلّوا العصر". | إشعار push (اختياري حسب إعداد المستخدم). |

لا نقدّم **دردشة نصية** في النسخة الحالية؛ التفاعل مقصود أن يكون بسيطاً (تشجيع، نبض، إشعارات محدودة) للحفاظ على التركيز على الصلاة وعدم إرهاق المستخدم.

### 3.3 ماذا نقول للمستخدم (أمثلة رسائل)

- **عند تسجيل الصلاة**: Toast "تم تسجيل الصلاة ✓" أو "بارك الله فيك".
- **عند تشجيع عضو**: للمُشجَّع: إشعار "فلان يرسل لك تشجيعًا 🌟". للمُرسل: Toast "تم إرسال التشجيع".
- **عند تسجيل الأب صلاة عن طفل**: Toast "تم تسجيل صلاة العصر لأحمد".
- **عند انضمام لعائلة**: "انضممت إلى عائلة [الاسم]. ستظهر نشاطاتهم هنا."
- **عند عدم وجود إنترنت**: "لا يوجد اتصال. سيتم حفظ البيانات ومزامنتها عند عودة الاتصال."

---

## 4. جميع الحالات والحدود (Edge Cases)

### 4.1 الموقع

| الحالة | السلوك المقترح |
|--------|-----------------|
| **المستخدم لم يفعّل الموقع** | استخدام مدينة افتراضية (من الإعدادات) أو آخر مدينة محفوظة. عرض رسالة واضحة في الإعدادات أو في الشاشة الرئيسية: "لم يتم تحديد موقعك — المواقيت حسب مدينة [X]. لتخصيص الموقع، فعّل خدمة الموقع أو اختر مدينة يدوياً." |
| **الموقع مرفوض نهائياً** | اعتماد اختيار المدينة يدوياً من الإعدادات (قائمة مدن أو إدخال اسم). حفظ الاختيار واستخدامه لمواقيت الصلاة. |
| **الموقع غير متاح (جهاز قديم أو خطأ)** | نفس سلوك "مدينة يدوياً" + عدم تعليق التطبيق — نعرض المواقيت من آخر موقع أو مدينة محفوظة. |

### 4.2 الإشعارات

| الحالة | السلوك |
|--------|--------|
| **صلاحيات الإشعارات مرفوضة** | عند محاولة جدولة إشعار أو عند فتح الإعدادات: عرض رسالة واحدة "لتفعيل تذكير الصلاة، يرجى السماح بالإشعارات من إعدادات التطبيق/الجهاز" مع زر يفتح إعدادات التطبيق. عدم إعادة الإزعاج في كل تشغيل. |
| **المستخدم أوقف "إشعارات الأذان"** | عدم إرسال أي إشعار أذان أو تذكير للصلاة؛ بقية التطبيق تعمل بشكل طبيعي. |
| **المستخدم أوقف "إشعارات العائلة"** | عدم إرسال إشعارات "فلان صلى"؛ يبقى Pulse داخل التطبيق يعمل. |

### 4.3 الشبكة والأوفلاين

| الحالة | السلوك |
|--------|--------|
| **لا إنترنت عند التسجيل** | حفظ التسجيل محلياً (SQLite) وإظهار Toast "تم الحفظ. سيتم المزامنة عند عودة الاتصال." عند عودة النت، SyncService يزامن تلقائياً. |
| **لا إنترنت عند فتح التطبيق** | عرض آخر بيانات محفوظة (مواقيت، عائلة، ملخص اليوم). إظهار مؤشر "غير متصل" (ConnectionStatusIndicator) دون منع الاستخدام. |
| **فشل المزامنة** | تسجيل الخطأ (AppLogger) وإظهار Toast عند إعادة المحاولة: "فشل المزامنة. سيتم إعادة المحاولة تلقائياً." |

### 4.4 العائلة والدعوات

| الحالة | السلوك |
|--------|--------|
| **المستخدم ليس في عائلة** | إظهار شاشة "انضم إلى عائلة أو أنشئ واحدة" (قائمة أو زر في التبويب). عدم إظهار Pulse أو إشعارات عائلية. |
| **انتهاء صلاحية كود الدعوة** | رسالة واضحة: "انتهت صلاحية الرابط. اطلب رابطاً جديداً من منشئ العائلة." |
| **المستخدم في أكثر من عائلة** | اختيار عائلة "الحالية" (مخزن في Storage/cache) وعرضها في تبويب العائلة؛ إمكانية التبديل لاحقاً من الإعدادات أو من واجهة العائلة. |

### 4.5 أول استخدام وتجربة المستخدم

| الحالة | السلوك |
|--------|--------|
| **أول فتح للتطبيق** | Onboarding: ترحيب → مميزات → طلب صلاحيات (موقع، إشعارات) → إعداد الملف الشخصي → ثم تسجيل الدخول أو إنشاء حساب. |
| **بعد تسجيل الدخول لأول مرة** | إن لم يكن في عائلة: توجيه لشاشة "انضم أو أنشئ عائلة" (اختياري أو مطلوب حسب المنتج). |
| **لا توجد صلوات مسجلة اليوم** | عرض "لم تُسجّل أي صلاة اليوم" مع زر سريع "سجّل الآن" أو عرض الصلاة الحالية من LiveContext. |

---

## 5. الإعدادات ومتى نطلبها

### 5.1 ترتيب الطلبات (Onboarding)

1. **اللغة** — من الشاشة الأولى أو من الإعدادات.
2. **الموقع** — خلال Onboarding مع شرح: "لحساب مواقيت الصلاة بدقة حسب مكانك." إن رُفض: ننتقل لاختيار المدينة لاحقاً.
3. **الإشعارات** — بعد الموقع أو معه: "نذكّرك بمواقيت الصلاة والتذكير مرة واحدة بعد الأذان." إن رُفض: نعرض الخيار مرة واحدة من الإعدادات دون إلحاح.
4. **الملف الشخصي** — الاسم، الجنس، تاريخ الميلاد (للعمر)، خلال Onboarding أو في أول دخول للوحة التحكم.

### 5.2 شاشة الإعدادات (مصدر واحد للقيم)

- **عام**: اللغة، المظهر (فاتح/غامق/حسب النظام).
- **الموقع**: "استخدام موقعي الحالي" أو "اختيار مدينة يدوياً" مع قائمة/بحث.
- **الإشعارات**:  
  - تفعيل/إيقاف كلي.  
  - أذان: تفعيل/إيقاف، وضع الصوت (أذان / اهتزاز / صامت).  
  - تذكير: تفعيل/إيقاف، عدد الدقائق بعد الأذان (مثلاً 30).  
  - إشعارات العائلة: تفعيل/إيقاف.
- **العائلة**: اختيار العائلة الحالية (إن وُجدت أكثر من واحدة)، أو الخروج من العائلة (مع تأكيد).
- **الخصوصية**: من يرى نشاطي (عائلة فقط، مجهول، إلخ) — إن وُجد نموذج في التطبيق.

يُفضّل أن تُقرأ كل القيم من Storage/Service واحد وتُعرض في واجهة واحدة حتى لا يتشتت المستخدم.

---

## 6. تحسين التطبيق تقنياً (ملفات وحجم وأداء)

### 6.1 مبادئ التقسيم

- **Feature-first**: كل ميزة في مجلدها (auth, family, prayer, notifications, settings, …) مع تقسيم داخلي إلى `data` (models, repos, services) و `presentation` (screens, widgets, bindings) و `controller` إن لزم.
- **ملف واحد = مسؤولية واحدة**: تجنب الملفات التي تتجاوز 300–400 سطر؛ تقسيم الشاشات الكبيرة إلى widgets فرعية في ملفات منفصلة.
- **مصدر واحد للثوابت**: استخدام `core/constants/constants.dart` (أو الملفات المعاد تصديرها) لـ API، StorageKeys، Enums، Dimensions.

### 6.2 الإشعارات في الكود

- **NotificationService**: يبقى مسؤولاً عن: تهيئة القنوات، صلاحيات النظام، جدولة الأذان والتذكير، إلغاء الإشعار عند التسجيل.
- **SmartNotificationService**: يركّز على أزرار الإشعار (صليت، لاحقاً) ومعالجة الـ payload وإرسال الأحداث إلى Stream أو إلى PrayerRepository/LiveContext.
- **تجنب التكرار**: دمج منطق "متى نجدول" و"متى نلغي" في مكان واحد (مثلاً في NotificationService مع استدعاء SmartNotificationService للأزرار فقط)، حتى لا نجدول من مكانين مختلفين.

### 6.3 التعامل مع الأخطاء والسجلات

- استخدام **AppLogger** في كل مكان يتم فيه catch (repos, services). عند فشل شبكة أو Firestore: تسجيل الخطأ ثم إظهار رسالة مفهومة للمستخدم عبر **AppFeedback** (Toast).
- عدم ترك المستخدم أمام شاشة بيضاء أو رسالة تقنية؛ دائماً رسالة مثل "حدث خطأ. حاول مرة أخرى." أو "تحقق من الاتصال."

### 6.4 تقليل حجم الملفات

- **شاشات كبيرة**: فصل الأقسام إلى widgets (مثلاً `DashboardPrayerCard`, `DashboardFamilySummary`, `FamilyPulseSection`) في ملفات منفصلة.
- **Controllers**: إن تجاوز الـ Controller 400 سطر، استخراج المنطق إلى UseCase أو Service (مثلاً `PrayerScheduleUseCase`) واستدعاؤه من الـ Controller.
- **Models**: إبقاء النماذج في ملفات منفصلة (مثلاً notification_models في مجلد notifications/data/models) وتصديرها من barrel file إن لزم.

---

## 7. خارطة نجاح التطبيق (ملخص تنفيذي)

| الركيزة | الإجراء |
|---------|---------|
| **وضوح القيمة** | المستخدم يفهم من أول استخدام: "تذكير صلاة + عائلة تشجع بعضها" دون تعقيد. |
| **إشعارات ذكية** | أذان في وقته، تذكير واحد فقط إن لم يُسجّل، إلغاء فوري عند التسجيل، أزرار "صليت" و"لاحقاً". |
| **تفاعل اجتماعي بسيط** | Pulse + تشجيع + إشعارات عائلية اختيارية — بدون دردشة. |
| **كل حالة مُدارة** | موقع، إشعارات، أوفلاين، أول استخدام، أخطاء — كلها لها رسالة وسلوك واضح. |
| **إعدادات واحدة** | مصدر واحد لتفضيلات الإشعارات والموقع والعائلة، مرتبط بواجهة إعدادات واحدة. |
| **كود قابل للصيانة** | تقسيم feature-first، ملفات أصغر، ثوابت وlogger موحّدان. |

بهذه المواصفة يكون التطبيق جاهزاً لنموذج متكامل: **نظام إشعارات واضح، تواصل محدد بين المستخدمين، وتعامل صريح مع كل الحالات** لتحقيق هدف "ربط الناس" واستخدامهم الفعلي للتطبيق.

---

## 8. ربط المواصفة بالكود الحالي (مرجع للمطور)

| البند في المواصفة | الملف/الخدمة في المشروع |
|-------------------|--------------------------|
| جدولة الأذان والتذكير وإلغاؤها | `NotificationService`, `DashboardController` (أو من يجدد الجدولة) |
| أزرار الإشعار (صليت / لاحقاً) | `SmartNotificationService`, معالجة `onDidReceiveNotificationResponse` |
| إعدادات الإشعار (صوت، تفعيل، عائلة) | `StorageKeys`, `StorageService`, شاشة الإعدادات |
| Pulse العائلة | `FamilyService`, `FamilyPulseEvent`, `family_dashboard_screen` (قسم النبض) |
| تشجيع عضو | `FamilyService.pokeMember` أو ما يعادله، FCM/إشعار للمُشجَّع |
| تسجيل الأب صلاة عن طفل | `FamilyService.logPrayerForMember` |
| حالة الموقع والمدينة اليدوية | `LocationService`, `SelectedCityController`, إعدادات الموقع |
| أوفلاين والمزامنة | `PrayerRepository`, `SyncService`, `DatabaseHelper`, `ConnectionStatusIndicator` |
| Onboarding والصلاحيات | `OnboardingController`, شاشات الترحيب والصلاحيات واختيار المدينة |
| تسجيل الأخطاء ورسائل المستخدم | `AppLogger`, `AppFeedback` / `ToastService` |
