# نسخة شبه نهائية (Beta) — قائمة التحقق

## ✅ ما تم التحقق منه لبناء الـ APK

| البند | الحالة |
|--------|--------|
| إصدار التطبيق (pubspec) | `1.0.0+1` |
| اسم التطبيق (Android) | صلاة |
| minSdk | 26 (مناسب للإشعارات المجدولة) |
| MultiDex | مفعّل |
| التوقيع (Release) | توقيع debug مؤقت — للاختبار فقط |
| بناء APK | نجح ✓ |

## 📦 مكان ملف الـ APK

```
build\app\outputs\flutter-apk\app-release.apk
```

الحجم تقريباً: ~132 MB (يمكن تقليله لاحقاً بـ `--split-per-abi` إذا أردت).

## 📲 تثبيت على الجهاز للتجربة أسبوع

1. انقل `app-release.apk` إلى الجوال (USB أو مشاركة ملفات).
2. على الجوال: فعّل "السماح من هذا المصدر" (مصادر غير معروفة) إن طُلب.
3. افتح الملف وثبّت التطبيق.
4. إن كان التطبيق القديم (debug) مثبتاً، قد تحتاج إلغاء تثبيته أولاً أو استخدام نفس التوقيع.

## ⚠️ ملاحظات للنسخة شبه النهائية

- **Firebase / Google Sign-In**: إذا ظهر `DEVELOPER_ERROR` أو "Unknown calling package" فهذا عادة من إعدادات Console (SHA-1 للتوقيع). للتجربة المحلية يمكن تجاهله؛ للتسجيل والدخول بالـ Google ستحتاج إضافة بصمة توقيع الـ debug (أو release لاحقاً) في Firebase Console.
- **التوقيع**: هذا الـ APK موقع بمفتاح debug. لنسخة نهائية أو لنشر على المتجر ستحتاج إنشاء keystore وتوقيع release (راجع `docs/PLAY_STORE_READINESS.md`).
- **الخصوصية**: تأكد من وجود رابط سياسة الخصوصية إذا سجّلت الدخول أو جمعت بيانات.

## ما يمكن تجربته خلال الأسبوع

- أوقات الصلاة والموقع.
- تسجيل الصلوات والـ Live Context (الشاشة الرئيسية).
- الإشعارات (أذان / تذكير).
- العائلة (إن كان الخادم متاحاً).
- تغيير المدينة والإعدادات.
- الاستقرار والتصفح (بدون overflow).

تم إنشاء هذا الملف بتاريخ البناء.
